/* Estilos gerais para a página */
body {
    background-color: #2d8f46; /* Cor verde de fundo */
    color: white;
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
}

/* Estilo para o cabeçalho */
header {
    text-align: center;
    padding: 20px;
    background-color: #1f6d32; /* Tom de verde mais escuro para o cabeçalho */
}

h1 {
    font-size: 3em;
    margin: 0;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
}

/* Estilos para o conteúdo principal */
.content {
    padding: 20px;
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
}

/* Estilo para os títulos dentro do conteúdo */
h2 {
    font-size: 2em;
    margin-top: 20px;
}

/* Estilo para os parágrafos */
p {
    font-size: 1.2em;
    line-height: 1.6;
    margin: 10px 0;
}

/* Estilo para o botão */
.button {
    background-color: #ffcc00;
    border: none;
    color: white;
    padding: 15px 25px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 1.2em;
    margin-top: 20px;
    cursor: pointer;
    border-radius: 5px;
    transition: background-color 0.3s ease;
}

.button:hover {
    background-color: #ffb300;
}

/* Estilo para o rodapé */
footer {
    text-align: center;
    padding: 20px;
    background-color: #1f6d32;
    margin-top: 20px;
}

/* Estilo para o texto do rodapé */
footer p {
    font-size: 1em;
    margin: 5px 0;
}

footer strong {
    color: #ffcc00;
}
